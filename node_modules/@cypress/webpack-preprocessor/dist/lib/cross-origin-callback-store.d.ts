export interface CrossOriginCallbackStoreFile {
    inputFileName: string;
    outputFilePath: string;
    source: string;
}
export declare class CrossOriginCallbackStore {
    private files;
    addFile(sourceFilePath: string, file: CrossOriginCallbackStoreFile): void;
    hasFilesFor(sourceFiles: string[]): boolean;
    getFilesFor(sourceFiles: string[]): CrossOriginCallbackStoreFile[];
    reset(sourceFilePath: string): void;
}
export declare const crossOriginCallbackStore: CrossOriginCallbackStore;
